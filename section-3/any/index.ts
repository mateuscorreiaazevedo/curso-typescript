// Any transmite ao Typescript qualquer tipo de dado
// Evitar ao máximo este tipo

// 2 casos: tipo de dado não importante, arrays com dados de múltiplos tipos

const list: any = [1, 'teste', true, [], {key: 1}]

console.log(list);
